# 把摩擦放對地方

> AI 真正降低的，是流程中的摩擦力。而摩擦有三種，只有一種該全部交給 AI。

最近我在想一件事。當我們導入 AI Agent 時，有一個很重要卻經常被跳過的概念：降低摩擦力。

我們通常會問：這個工作能不能交給 AI？更值得問的可能是另一個問題：在整個工作流程裡，人會在哪些地方停下來、等待、切換工具、搬運資料、重複確認，或者因為資訊不足而卡住？

這些地方就是流程中的摩擦力。AI Agent 的價值在於找出流程中的阻力，讓資料、判斷與產出可以順暢地往下一個環節移動。完成某一項工作，只是其中一部分。

## 什麼是流程中的摩擦力

先給一個可以拿去實際盤點的定義：流程中每一個需要人主動介入、流程才能繼續往前的地方，都是摩擦力。

每一次等待、複製貼上、重新輸入、切換系統、尋找檔案、補充背景、核對格式，都符合這個定義。單看其中一個動作可能只要幾十秒，感覺沒什麼。但當一個流程要經過五個系統、處理十個檔案，而且每天重複很多次，這些微小的阻力就會累積成大量的時間、溝通與認知成本。

其中認知成本最容易被低估。切換視窗、重新想起上一步做到哪裡、確認格式對不對，這些都會佔用注意力，而注意力是一天之內最有限的資源。

降低摩擦力，就是減少這些停頓，讓流程跑得更快、更穩定，也更省人的注意力。

## 這跟以前的自動化差在哪裡

先把時間軸拉開。自動化其實走過三個階段，混在一起談會失焦。

**第一階段是規則式程式。** 輸入固定、規則明確、可以事先寫成條件判斷的工作，交給程式最划算。欄位不能空白、金額不能小於零、日期必須符合特定格式，只要規則能事先定義，程式就能快速而穩定地執行。

**第二階段是專用模型。** OCR、影像辨識、垃圾郵件分類這類技術已經存在很多年，它們處理的正是規則寫不完整的工作。代價是每一個任務都要單獨蒐集資料、單獨訓練、單獨調校。導入成本高，所以只有量夠大的場景才划算。

**第三階段是通用模型與 Agent。** 真正改變的地方在於，同一個模型用自然語言描述任務就能得到堪用的結果，不必為每個任務重新訓練。門檻掉下來之後，很多以前規模不夠、不值得單獨開發模型的工作，現在也進得了自動化的範圍。

所以比較準確的說法是：程式把已知規則自動化，專用模型把特定的模糊判斷自動化，通用模型與 Agent 則讓「規則寫不完整，但人看了大致知道該怎麼判斷」的工作，第一次便宜到可以隨手導入。

這也改變了流程設計的方式。以前是人把每一個步驟先寫死，再讓程式照著執行。現在可以由人說明目標、限制與驗收條件，讓 Agent 自己尋找資料、選擇工具並完成工作。

## 同樣能對話，為什麼摩擦差這麼多

這可以解釋一件事：為什麼有些 AI 工具模型能力明明很強，實際工作時還是讓人覺得卡。

有人會說，用傳統的 Chat 介面一樣可以完成整套工作。我可以把 Excel 貼到對話裡請它分析，分析完成後把結果下載下來，貼到下一個對話或系統，繼續處理後面的工作。

這確實是一種可行的 AI 工作流，也已經比完全不用 AI 有效率。問題出在每一段工作之間：仍然需要一個人負責搬運資料。

這個人必須先找到正確的檔案，把檔案貼進對話，說明背景，等待分析完成，再把結果下載或複製出來。接著確認輸出的格式，把結果貼進下一個系統，重新補充下一段工作需要的背景。

每一個動作都不困難，卻會讓人不斷停下來、切換視窗、重新建立上下文。

只要流程還依賴人來搬運資料，人就成了整套系統的**人工 API**：負責在兩個彼此不相通的系統之間，用手把資料轉成對方吃得下的格式。AI 完成了其中的分析工作，整條流程卻還沒有真正接起來。

這就是李校長說的：從 Chat 走向 Cowork 或 Work。

Chat 的基本邏輯是，你把資料交給 AI，AI 把答案交還給你。Work 或 Agent 模式的基本邏輯是，你說明目標，AI 自己取得需要的資料、使用工具完成任務、產生檔案，並把結果接到下一個工作環節。

舉個例子。我們不必先手動找到三份 Excel 再逐一貼進對話。只要 Agent 已經取得適當的檔案存取權限，就可以直接告訴它：

> 讀取這個資料夾裡本週的銷售資料，跟上週比較，找出異常項目，整理成一份報告，並產生一份給主管使用的簡報。

接下來 Agent 可以自行讀取檔案、整理資料、執行分析、產生圖表，再輸出報告與簡報。完成的檔案直接留在工作區，供下一段流程繼續使用。

這時人只要在關鍵位置確認：Agent 用了哪些來源、分析方法是否合理、異常判斷是否正確，以及產出能不能交給下一個流程。

所以 Chat 與 Work 的差異在於**摩擦發生的位置**。Chat 降低的是單一任務裡的思考與產出摩擦，Work 進一步降低任務之間的交接與資料搬運摩擦。

如果 AI 只能回答問題，卻讀不到工作現場、用不了工具、改不了檔案、交不出成果，人就必須一直擔任中間的搬運者。一套真正低摩擦的 Agent 工作流，會讓人從搬運資料的角色，轉成定義目標、設定邊界與驗收結果的角色。

## 摩擦會轉移，未必消失

AI 並不會自動讓流程更可靠。它會消除舊的錯誤，同時引入新的錯誤。

OCR 可以減少人工輸入資料的時間，也能降低打字錯誤，但 OCR 本身會辨識錯誤。如果辨識結果沒有經過欄位檢查就直接進入下一個系統，錯誤會以自動化的速度往下游擴散，而且因為中間沒有人經手，更難被發現。摩擦被移走了，風險留了下來。

影像辨識也一樣。AI 可以先篩掉大量正常圖片，減少人工檢查的數量，但它也會漏掉少數真正異常的影像。

這裡有一個設計上的取捨值得講清楚：初篩型的 AI 應該把門檻調成**寧可多報、少漏報**。多報的代價是人多看幾張圖，漏報的代價是異常直接流到下游。兩者不對等，所以門檻要往多報那一側偏。

比較精確的說法是：AI 可以降低手動輸入、資料搬運、搜尋與初篩造成的錯誤。整體錯誤率會不會下降，取決於後面的驗證與例外處理設計得好不好。

這裡有一個常見的失敗模式。如果 AI 做完一次，人還要把全部內容重新檢查一次，最後會變成 AI 做一遍、人再做一遍，形成雙重工作。

避開它的關鍵，是給出一個明確的判準，決定哪些結果需要人看。實務上可用的判準有三種：

- 模型信心分數低於門檻的
- 金額或數量落在高風險區間的
- 固定比例的隨機抽檢

這三種以外的結果直接放行，並保留紀錄供事後追查。

好的自動化會減少人需要檢查的數量，並把最值得看的東西排在最前面。

## 有些地方應該刻意增加摩擦

談流程改善時，很容易把所有摩擦都當成壞事，好像流程越順、步驟越少、自動化程度越高就越好。

在安全、治理與高風險決策上，適度的摩擦是一種保護機制。AI 可以辨識發票內容，但金額超過門檻時要經過人工確認。AI 可以產生對外文件，但正式發布前要由負責人審查。AI Agent 可以修改資料，刪除大量檔案則需要事前批准。

這些確認會讓流程變慢，看起來像在增加摩擦，但它們把錯誤攔在被放大到下游之前。

增加摩擦也可以靠硬性的系統條件，不必全部依賴人工審查：

- 欄位與資料格式必須通過驗證
- 金額、數量與日期必須落在合理範圍
- AI 信心不足時強制轉交人工處理
- 涉及機密資料前先檢查使用者權限
- 執行刪除、發布或付款等不可逆操作前再次確認
- 系統保留操作紀錄，讓結果可以追蹤與回復
- AI 產出的程式通過測試後才能進入正式環境

這些檢查都會增加流程阻力，它們是刻意設計的安全邊界。

比較合理的分工是：**確定的事情交給程式，模糊的事情交給 AI，關鍵判斷留給人。**

## 還有一種摩擦值得主動保留

除了治理與安全之外，另一種摩擦也不該被 AI 消除：有意義的摩擦。

有些麻煩純粹是浪費，例如重複輸入資料、搬運檔案、整理固定格式。這些交給 AI 通常沒有問題。

但有些麻煩，是思考、學習與創造真正發生的地方。當我們試著把一個模糊想法說清楚、親自判斷不同方案的取捨，或者用自己的話重新整理一段知識，這些過程都不輕鬆。它們會讓人停下來、感到不確定，甚至產生挫折。

正是這些阻力，迫使我們形成自己的理解。

如果每次遇到問題都直接向 AI 要答案，我們確實可以更快得到一個看起來完整的結果，卻跳過了形成判斷的過程。久而久之，得到的答案越來越多，自己的判斷力卻不一定跟著成長。

AI 最好的角色，是移除低價值的阻力，讓我們有更多時間處理真正值得思考的問題。它可以幫忙整理資料、尋找矛盾、提出反例、模擬不同方案。最後要相信什麼、選擇什麼、願意承擔什麼結果，仍然由人決定。

## 怎麼找出自己流程裡的摩擦點

概念要能落地，需要一個具體的動作。

挑一條你每週都要跑好幾次的流程，把它從頭到尾寫下來，然後在每一個步驟旁邊標三件事：

1. 這一步需要人做什麼？
2. 如果做錯了，多久會被發現？
3. 發現之後能不能回復？

需要人做的動作裡，只做搬運與轉換的那些，是最先該交給 Agent 的。錯了很久才會被發現、而且回復不了的那些步驟，則應該加上確認。剩下需要你親自想清楚的，留著。

## 把摩擦放對地方

AI 時代的流程設計，追求的是把摩擦放在對的位置，而不是把摩擦全部拿掉。三種摩擦，三種處理方式：

| 摩擦類型 | 特徵 | 怎麼處理 |
| --- | --- | --- |
| 低價值摩擦 | 重複、可逆、只是搬運與轉換 | 盡量交給 AI |
| 治理摩擦 | 高風險、不可逆、涉及權限與責任 | 刻意增加 |
| 有意義的摩擦 | 關乎理解、創造、學習與判斷力 | 主動保留 |

導入 AI Agent 因此更像是重新設計整條流程：哪些地方應該暢通無阻，哪些地方必須減速確認，哪些地方值得讓人親自走過。

好的 AI 系統，是把摩擦放對地方的系統。
